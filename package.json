{
  "name": "ai-storytelling-project",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/index.js",
    "backend-install": "cd backend && npm install",
    "frontend-build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "20.14.0"
  }
}